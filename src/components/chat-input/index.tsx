import { ChatInputContainer, InputOverride } from "./chat-input.styles";

const ChatInput = () => {
  return (
    <ChatInputContainer>
      username:
      <InputOverride />
    </ChatInputContainer>
  );
};

export default ChatInput;
